This test allocates a queue, writes DEADBEEF into it, reads the queue and confirms that the data is read out correctly as expected.

Can use some expanding to test return values from reading an empty queue or other errors, also would be good to write more data
to the queue or other tests.
