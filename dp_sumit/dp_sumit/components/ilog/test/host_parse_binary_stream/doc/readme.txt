This is a very basic test to test the __ilog function.

It will parse the messages into a human viewable format from a serial stream
from stdin.  It is designed to receive input from the other test program
host_output_binary_stream

